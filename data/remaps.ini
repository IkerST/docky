# Docky remap file
#
# This file allows mapping exec strings to each other.
# With this file, apps that have a script 'foo' that then winds up
# executing some executable 'bar' can be properly matched.
#
# The format is:
#
#   key=val
#
# where key gets mapped to val.
#
Picasa3.exe=picasa
nbexec=netbeans
deja-dup-preferences=deja-dup
VirtualBox=virtualbox
